/* eslint-disable */
import Moment from 'moment'
import * as momentRange from 'moment-range'
import date from 'constants/date'

let newMoment = momentRange.extendMoment(Moment)

export default newMoment
